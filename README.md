# 客服话术查询工作台

搜 SKU 找话术，一键复制。纯静态前端（HTML + JS + JSON），数据与页面同仓库加载，无需后端。

## 使用

直接打开 GitHub Pages 网址即可：`https://runtang-jerrold.github.io/kefu-huashu-workbench/`

- 搜 SKU 编码或关键词
- 按店铺、分类筛选
- 点击卡片复制回复内容

## 数据更新

修改 `data.json` 并提交到 main 分支，Pages 会自动部署。数据管理全走对话，不做前端编辑。
